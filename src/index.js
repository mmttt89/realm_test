import React from 'react';
import {View} from "react-native";
import RootNavigator from "@Navigations/Root";

const Root = () =>
    <View style={{ flex: 1 }}>
        <RootNavigator />
    </View>

export default Root;
